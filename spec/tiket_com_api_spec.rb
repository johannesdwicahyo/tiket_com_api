require 'spec_helper'

describe TiketComApi do
  it 'has a version number' do
    expect(TiketComApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
